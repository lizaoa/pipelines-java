parameters:
- name: 'solution'
  default: 'eee'
  type: string

steps:
- bash: |
    if [ -z '${{ parameters.solution }}' ]; then
      echo IS NULL AND $SOLUTION
    fi
  env:
    SOLUTION: ${{ parameters.solution }}
  displayName: Check for required parameters
- bash: echo ${{ parameters.solution }}---$SOLUTION
