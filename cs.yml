parameters:
- name: 'solution'
  default: 'ggg'
  type: string

steps:
- bash: |
    if [ -z $SOLUTION ]; then
      echo IS NULL AND $SOLUTION
    fi
  env:
    SOLUTION: ${{ parameters.solution }}
  displayName: Check for required parameters
  name: stna
- bash: echo ${{ parameters.solution }}--$sta.SOLUTION---$(stna.SOLUTION)--$env:SOLUTION--$stna.env:SOLUTION--%SOLUTION%
