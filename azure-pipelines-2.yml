pool:
  name: my-win-pools

trigger:
  - none

parameters:
- name: myString
  type: string
  default: a string
- name: myObject
  type: object
  default:
  - one
  - two
    # foo: FOO
    # bar: BAR
    # things:
    # - one
    # - two
    # - three
    # nested:
    #   one: apple
    #   two: pear
    #   count: 3


steps:
- ${{ each avalueAn in parameters.myObject }}:
  - script: echo ${{ avaluean }}
