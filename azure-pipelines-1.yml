pool:
  name: my-win-pools

trigger:
  - none

parameters:
- name: myString
  type: string
  default: a string
- name: myObject
  type: object
  default: 
    - a1: 111
    - b1: 222         
steps:
- ${{ each step in parameters.myobject }}:
    - ${{ if eq( step.a1,'111') }}:
          - script: echo gggg -- ${{ step.a1 }}