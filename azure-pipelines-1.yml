pool:
  name: my-win-pools

trigger:
  - none

variables:
  asd: 111
  bef: Nbaa NNB ${{parameters.mystring}}--
  
parameters:
- name: myString
  type: string
  default: a string
- name: myObject
  type: object
  default: 
    - a1: 111
    - b1: 222         


steps:
- script: echo this is ${{variables.bef}}