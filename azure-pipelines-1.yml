pool:
  name: my-win-pools

trigger:
  - none

parameters:
- name: myString
  type: string
  default: a string
- name: myObject
  type: object
  default: 
    bash:
        aaaa:
           bbb:vvv            

steps:
- ${{ if eq(parameter.myobject.bash.aaaa.bbb,'vvv') ) }}:
          - script: echo gggg --