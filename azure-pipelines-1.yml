# Repo: Contoso/WindowsProduct
# File: azure-pipelines.yml
variables:
  - group: vgtest

pool:
  name: my-win-pools

trigger:
   none

resources:
  repositories:
    - repository: Myrep
      type: github
      name: lizaoa/az_group
      #ref: refs/tags/v1.0 # optional ref to pin to
      endpoint: TestProj

steps:
  - checkout: Myrep
  - script: dir $(build.sourcesdirectory)
  - script: type comm.yml
  - script: git checkout v1.0
  - script: type comm.yml
