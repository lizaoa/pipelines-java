# File: azure-pipelines.yml


trigger:
- nb

pool:
  name: my-win-pools
parameters:
- name: yesno
  type: boolean
  default: true
steps:
  - template: sim.yml
  
  - script: echo disp is ${{ parameters.yesno }}
