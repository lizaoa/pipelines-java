# File: azure-pipelines.yml


trigger:
- nb

pool:
  name: my-win-pools
parameters:
- name: yesno
  type: boolean
  default: false

steps:
  - ${{ if eq(parameters.yesno,true) }}:
    - bash: echo "it is true"
