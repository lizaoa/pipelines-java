pool:
  name: my-win-pools

trigger:
  - none

parameters:
- name: myString
  type: string
  default: a string
- name: myObject
  type: object
  default: 
     foo: 
       - FOO1:001
       - FOO2:002
       - FOO3:003
     bar: BARS




steps:
- ${{ each a in parameters.myObject }}:
      - script: echo ${{ a.value.FOO1 }}