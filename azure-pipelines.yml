# File: azure-pipelines.yml


trigger:
- nb

pool:
  name: my-win-pools
parameters:
- name: yesno
  type: boolean
  default: false
steps:
- template: sim.yml
  parameters:
    yeSno: true
  
- script: echo disp is ${{ parameters.yesno }}
