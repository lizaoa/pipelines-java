pool:
  name: my-win-pools

trigger:
  - none

parameters:
- name: myString
  type: string
  default: a string
- name: myObject
  type: object
  default: 
    - one: 1
    - two: 2
    - three: 3

steps:
- ${{ each a in parameters.myObject }}:
      - ${{ if eq(a.key,'three') }}:
#        - ${{ each b in a.value }}:      
          - script: echo ttttt  ----${{ a.value }}